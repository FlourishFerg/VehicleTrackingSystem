apiVersion: v1
kind: Service
metadata:
  name: api-gateway-service
spec:
  selector:
    app: api-gateway
  type: LoadBalancer     # 👈 important!
  ports:
    - name: http
      port: 8088         # the port you want exposed
      targetPort: 8080   # must match your containerPort
